{
  "name": "@podcraft/ml",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --host 127.0.0.1 --port 5001 --reload",
    "start": "uv run uvicorn app.main:app --host 127.0.0.1 --port 5001",
    "build": "echo 'no build step'",
    "typecheck": "uv run ruff check app/ tests/",
    "lint": "uv run ruff check app/ tests/",
    "test": "uv run pytest --tb=short",
    "setup": "uv sync --all-extras"
  }
}
